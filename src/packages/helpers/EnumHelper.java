package packages.helpers;

/**
 * @author Andrii Dzhyrma and Liliia Chuba
 * 
 *         Class that helps to work enum type
 */
public class EnumHelper {
	/**
	 * Returns string array with all enum values
	 * @param values - enum values in an array
	 */
	/*public static <T extends Enum<T>> String[] enumToStringArray(T[] values) {
		int i = 0;
		String[] result = new String[values.length];
		for (T value : values) {
			result[i++] = value.name();
		}
		return result;
	}*/
}
